# Dependências da NOVA app (webapp/, FastAPI) — separado de requirements.txt
# de propósito, pra não mudar em nada o que o serviço Streamlit em
# produção instala (ver plano de migração em /root/.claude/plans).
fastapi==0.115.6
uvicorn[standard]==0.34.0
jinja2==3.1.5
python-multipart==0.0.20
cachetools==5.5.0

# Compartilhadas com a app antiga (mesma versão, sem motivo pra divergir)
supabase==2.7.4
bcrypt==4.2.0
fpdf2==2.8.1
requests==2.32.3
Pillow==11.1.0
