// Preload intencionalmente vazio: a página roda isolada (contextIsolation),
// sem acesso a APIs do Node/Electron. O app não precisa expor nada a mais
// pro site — ele só serve a interface que já existe no Streamlit.
